import { StyleSheet, Touchable, TouchableOpacity } from "react-native"

export default  CustomVideo = ({video}) =>{
    return(
        <TouchableOpacity style={styles.big_container}>

        </TouchableOpacity>
    )
}

const styles = StyleSheet.create({
    big_container:{
        height:'45%',
        width:'100%'
    }
})